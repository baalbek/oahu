apply plugin: 'java'

group = 'rcstadheim'

version = "$major.$minor.1"

repositories {
    mavenCentral()
}

def jarFile = new File("$libsDir/oahu-${version}.jar")

//*
task copyToParentProject(type: Copy, dependsOn: jar) {
    into parentDist from jarFile
}
//*/
